// Opencode hook generator (v1.7.0+, Phase 1).
//
// Writes a plugin JS file at .opencode/plugins/reversa-keeper.js.
// Plugin uses tool.execute.after for per-edit append + session.end for advisory.
//
// PreToolUse equivalent (tool.execute.before) removed in Phase 1.
// Returns in Phase 4 for policy gate.
//
// The plugin file is owned by Reversa — entire file is created/deleted.

import { existsSync, unlinkSync } from 'node:fs';
import { join } from 'node:path';

const PLUGIN_REL = '.opencode/plugins/reversa-keeper.js';

function buildPlugin(runnerPath) {
  return `// Auto-generated by \`npx reversa add-hooks --engine opencode\`.
// Manages the Keeper queue based on Opencode tool execution events.
// Removed by \`npx reversa remove-hooks --engine opencode\` or \`npx reversa uninstall\`.

import { spawn } from 'node:child_process';

const RUNNER = ${JSON.stringify(runnerPath)};

function invokeRunner(phase, payload) {
  const child = spawn('node', [RUNNER, '--phase', phase, '--engine', 'opencode', '--tool', payload?.tool ?? 'unknown'], {
    stdio: ['pipe', 'inherit', 'inherit'],
    detached: false,
  });
  try {
    child.stdin.write(JSON.stringify(payload ?? {}));
    child.stdin.end();
  } catch { /* swallow */ }
}

export default {
  'tool.execute.after': async ({ tool, input, output }) => {
    invokeRunner('post', { tool, tool_input: input, output });
  },
  'session.end': async () => {
    invokeRunner('stop', {});
  },
};
`;
}

export default {
  id: 'opencode',
  name: 'Opencode',

  describe() {
    return 'Installs plugin .opencode/plugins/reversa-keeper.js (tool.execute.after + session.end)';
  },

  generate({ projectRoot, runnerPath }) {
    const target = join(projectRoot, PLUGIN_REL);
    return {
      files: [{ path: target, content: buildPlugin(runnerPath) }],
      summary: [
        `Will write to: ${PLUGIN_REL}`,
        `  Plugin spawns runner via child_process on tool.execute.after + session.end.`,
      ].join('\n'),
    };
  },

  remove({ projectRoot }) {
    const target = join(projectRoot, PLUGIN_REL);
    const result = { removed: [], cleaned: [] };
    if (existsSync(target)) {
      try { unlinkSync(target); result.removed.push(target); }
      catch { /* swallow */ }
    }
    return result;
  },
};
